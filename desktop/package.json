{
  "name": "electron",
  "version": "0.1.0",
  "description": "electron app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-react": "cd ./desktop && npm start",
    "start-electron": "npx electronmon --trace-warnings .",
    "dev": "concurrently \"npm run start-react\" \"npm run start-electron\""
  },
  "keywords": [
    "electron",
    "react"
  ],
  "author": "MIT",
  "license": "MIT",
  "dependencies": {
    "electron": "^31.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
